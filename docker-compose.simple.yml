version: '3'

services:
  streamlit:
    build:
      context: .
      dockerfile: Dockerfile.simple
    ports:
      - "8501:8501"
    volumes:
      - ./:/app
    environment:
      - PYTHONUNBUFFERED=1
